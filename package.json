{
  "name": "squeaky-clean",
  "version": "1.0.0",
  "private": true,
  "description": "A house cleaning task tracker PWA",
  "scripts": {
    "dev": "cd frontend && npx vite --host",
    "build": "cd frontend && npx vite build",
    "preview": "cd frontend && npx vite preview",
    "worker:dev": "cd worker && npx wrangler dev",
    "deploy:pages": "cd frontend && npx vite build && npx wrangler pages deploy dist",
    "deploy:worker": "cd worker && npx wrangler deploy"
  }
}
